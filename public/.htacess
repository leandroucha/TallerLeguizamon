# Activar motor de reescritura
RewriteEngine On

# Servir archivos y directorios reales tal cual
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Enviar el resto al front controller
RewriteRule ^ index.php [QSA,L]
